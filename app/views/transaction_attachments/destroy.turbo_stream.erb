<%= turbo_stream.replace "transaction_attachments_#{@transaction.id}", partial: "transactions/attachments", locals: { transaction: @transaction, can_upload: Current.user.admin?, can_delete: Current.user.admin? } %>
<% flash_notification_stream_items.each do |item| %>
  <%= item %>
<% end %>
