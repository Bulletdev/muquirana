<%# locals: (account:, url:) %>

<%= render "accounts/form", account: account, url: url do |form| %>
  <%#
    Lookup ABSOLUTO de proposito, nao lazy. Este bloco e passado para o partial
    "accounts/form", e o t('.chave') resolve o @virtual_path em tempo de
    execucao -- que aqui ja e "accounts/form". Com lazy, as chaves viravam
    accounts.form.* e a tela renderizava "translation missing".
  %>
  <%# Ver o comentario em depositories/_form: o rotulo do subtipo tem uma fonte so. %>
  <%= form.select :subtype,
                 Investment::SUBTYPES.keys.map { |k| [Investment.long_subtype_label_for(k), k] },
                 { label: true, prompt: t("investments.form.subtype_prompt"), include_blank: t("investments.form.none") } %>
<% end %>
