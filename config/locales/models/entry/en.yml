---
en:
  activerecord:
    attributes:
      entry:
        amount: Amount
        date: Date
    errors:
      models:
        entry:
          attributes:
            base:
              invalid_sell_quantity: cannot sell %{sell_qty} shares of %{ticker} because
                you only own %{current_qty} shares
