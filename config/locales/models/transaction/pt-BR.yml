---
pt-BR:
  activerecord:
    errors:
      models:
        transaction:
          attributes:
            attachments:
              invalid_format: "o arquivo %{index} tem um formato não suportado (%{file_format})"
              too_large: "o arquivo %{index} excede o tamanho máximo de %{max_mb} MB"
              too_many: "não pode ter mais que %{max} anexos"
