---
en:
  activerecord:
    attributes:
      import:
        date_format: Date format
        currency: Currency
        number_format: Number Format
    errors:
      models:
        import:
          attributes:
            raw_file_str:
              invalid_csv_format: is not a valid CSV format
