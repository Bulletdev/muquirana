---
en:
  settings:
    hostings:
      invite_code_settings:
        description: Every new user that joins your instance of Muquirana can only
          do so via an invite code
        email_confirmation_description: When enabled, users must confirm their email
          address when changing it.
        email_confirmation_title: Require email confirmation
        generate_tokens: Generate new code
        generated_tokens: Generated codes
        max_uses_label: How many people
        title: Require invite code for signup
      external_assistant_settings:
        title: External AI Assistant
        description: Point the AI assistant at your own local LLM endpoint so your
          financial data never leaves your machine.
        env_var_configured: The external assistant is configured through the
          EXTERNAL_ASSISTANT_URL environment variable.
        privacy_title: Your data stays on your machine
        privacy_body: When an endpoint is set, chats are sent to your own server
          (Ollama, LM Studio, or a custom agent) instead of OpenAI. Leave the URL
          blank to fall back to the default assistant.
        protocol_note: "Protocol: OpenAI-compatible /v1/chat/completions with SSE
          streaming. Ollama's native /api/chat endpoint is not supported -- use the
          OpenAI-compatible port (e.g. http://localhost:11434)."
        url_label: Endpoint URL
        url_placeholder: http://localhost:11434
        model_label: Model
        model_placeholder: e.g. llama3.2
        token_label: API token (optional)
        token_placeholder: Leave blank for a local endpoint without auth
        agent_id_label: Agent ID (optional)
        agent_id_placeholder: For a custom agent; ignored by Ollama / LM Studio
      show:
        general: General Settings
        external_assistant: External AI Assistant
        invites: Invite Codes
        title: Self-Hosting
        danger_zone: Danger Zone
        clear_cache: Clear data cache
        clear_cache_warning: Clearing the data cache will remove all exchange rates,
          security prices, account balances, and other data. This will not delete
          accounts, transactions, categories, or other user-owned data.
        confirm_clear_cache:
          title: Clear data cache?
          body: Are you sure you want to clear the data cache? This will remove all
            exchange rates, security prices, account balances, and other data. This
            action cannot be undone.
      synth_settings:
        api_calls_used: "%{used} / %{limit} API calls used (%{percentage})"
        description: API key for the Synth instance that SYNTH_URL points to.
        discontinued_body_html: The original service went down along with the Maybe
          project, and there is no longer anywhere to create a key. This field stays
          for anyone running their own instance from the %{archive_link} and pointing
          %{env_var} at it. Without it, the app simply does not fetch exchange rates
          or security prices -- nothing breaks.
        discontinued_link: archived Synth code
        discontinued_title: Synth has been discontinued
        env_var_configured: You have successfully configured your Synth API key through
          the SYNTH_API_KEY environment variable.
        label: API Key
        placeholder: Enter your API key here
        plan: "%{plan} plan"
        title: Synth Settings
      update:
        failure: Invalid setting value
        success: Settings updated
      clear_cache:
        cache_cleared: Data cache has been cleared. This may take a few moments to
          complete.
      not_authorized: You are not authorized to perform this action
